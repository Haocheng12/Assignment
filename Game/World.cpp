#include "World.h"


//World::World() {
//    tiles.load();
//    for (int i = 0; i < maxSize; i++) {
//        a[i] = rand() % tileNum; // Assuming tileNum is defined in Tile.h or elsewhere
//    }
//}
//
//void World::draw(Window& canvas, int wy) {
//    int height = 384;
//    int Y = wy / height;
//    tiles[a[Y % maxSize]].draw(canvas, canvas.getHeight() / 2);
//    tiles[a[(Y + 1) % maxSize]].draw(canvas, 0);
//}
